#include <cstdio>
using namespace std;
int main() {
    int l,w,n,d;
    while(~scanf("%d%d%d%d",&l,&w,&n,&d)) {
        if(l+w+n+d==0) break;
        for(int i=0; i<n; i++) {
            scanf("%d%d",&x,&y);
        }
    }
    return 0;
}
