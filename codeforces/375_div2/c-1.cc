#include <cstdio>
#include <map>
using namespace std;
map<int,int> cnt;
int main() {
    int n, m;
    scanf("%d%d",&n,&m);
    for(int i=0; i<n; i++) {
        scanf("%d", &v[i]);
        cnt[v[i]]++;
        if(v[i]>m) idx.push_back(i);
    }
    int k=n/m;
    for(auto &p:idx) {
        if(p->first<=m&&it->second<k)
    }
    return 0;
}
