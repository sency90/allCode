#include <iostream>
using namespace std;
int main() {
    int n,k,p; scanf("%d%d%d",&n,&k,&p);
    for(int i=0; i<n; i++) {
        scanf("%d", &v[i]);
    }
    sort(v,v+n);
    for(int i=0; i<m; i++) {
        scanf("%d", &key[i]);
    }
    sort(key,key+m);

    for(int i=0; i)

    return 0;
}
