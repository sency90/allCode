#include <stdio.h>
int main() {
    freopen("csin.txt", "r", stdin);
    freopen("csout.txt", "w", stdout);
    int t;
    scanf("%d", &t);
    for(int i=1; i<=t; i++) {

    }
    fclose(stdin);
    return 0;
}
