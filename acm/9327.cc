#include <cstdio>
using namespace std;
int main() {
    int tc;
    scanf("%d", &tc);
    while(tc--) {
        scanf("%d%d",&n,&e);
        for(int i=0; i<n; i++) {
            scanf("%d", &s[i]);
        }

    }
    return 0;
}
