#include <stdio.h>
int main() {
    int t;
    scnaf("%d", &t);
    double x;
    while(t--) {
        scanf("%d", x);
        while((c=getchar())!='\n' && c!='\r' && c!=EOF) {
            if(c==' ') continue;
            switch(ccp)
        }
    }
    return 0;
}
