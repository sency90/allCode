#include <stdio.h>
#include <algorithm>
#define MOD 1000000007
#define HASH 100007
using namespace std;
int q[100];
int main() {
    int n;
    long long x0, a, b;
    scanf("%d%d%d%d",&n,&x0,&a,&b);
    scanf("%d", &m);
    for(int i=0; i<m; i++) {
        scanf("%d", &q[i]);
    }
    for(int i=0; i<=n; i++) {

    }
    sort(q,q+m);
    for(int i=0; i<m; i++) {

    }


    return 0;
}
