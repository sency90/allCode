#include <cstdio>
using namespace std;
char s[27], mp[300][301];
int main() {
    int n,m,l;
    scanf("%d%d%d",&n,&m,&l);
    scanf("%s",s);
    for(int i=0; i<n; i++) {
        scanf("%s", mp[i]);
    }
    return 0;
}
