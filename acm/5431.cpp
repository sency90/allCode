#include <stdio.h>
int main() {
    int t;
    scanf("%d", &t);
    while(t--) {
        scanf("%d", &n);
        for(int i=0; i<n; i++) {
            scanf("%d", &v[i]);
            scanf("%d", &d);
            
        }
    }
    return 0;
}
