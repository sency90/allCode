#include <cstdio>
using namespace std;
int main() {
    int n; scanf("%d", &n);
    v.push_back(i);
    for(int i=2; i<n; i++) {
        for(int j=0; j<2; j++) {
            v.push_back(i);
        }
    }
    return 0;
}
