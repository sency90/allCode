#include <cstdio>
using namespace std;
int main() {
    freopen("in2.txt", "r", stdin);
    freopen("out2.txt", "w", stdout);
    int d;
    while(~scanf("%d", &d)) {
        printf("%d, ", d);
    }
    return 0;
}
